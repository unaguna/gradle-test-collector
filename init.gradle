rootProject {
    task 'projectlist' {
        doFirst {
            def output = file(getProperty("testcollector.prjoutput"))
            allprojects {
                output.append "${project.path} ${projectDir.absolutePath}\n"
            }
        }
    }
}
