rootProject {
    task 'projectlist' {
        doFirst {
            def output = file(getProperty("jp.unaguna.prjoutput"))
            allprojects {
                output.append "${project.path} ${projectDir.absolutePath}\n"
            }
        }
    }
}
